# Example environment variables (do NOT store real secrets in the repository)
# Copy to .env and fill in values for local development when running a backend server.

OPENAI_API_KEY=

# Note: For client-side (browser) testing this project stores a user-provided key in localStorage.
# Production: never put secret keys in client-side code; use a server-side proxy.